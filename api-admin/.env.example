# Server
PORT=3002
NODE_ENV=development

# Database (techres_master - for master data)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=techres_master

# JWT (used for internal API validation, main auth via api-oauth)
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d
